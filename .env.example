VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_X_TBA_AUTH_KEY=
VITE_NEXUS_API_KEY=
VITE_NEXUS_TOKEN=
# Public URL of the mobile web app — used as the base for auth email links (verify/reset).
# Set to https://funkyscout.vercel.app in production.
# In dev, set to your ngrok URL or LAN IP (e.g. http://192.168.x.x:5713) so testers can click email links.
# If unset, falls back to window.location.origin (only works for same-machine access).
VITE_REDIRECT_URL=
